apiVersion: apps/v1
kind: Deployment
metadata:
  name: guestbook
  namespace: guestbook-staging
spec:
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: guestbook
  template:
    metadata:
      labels:
        app: guestbook
    spec:
      containers:
      - image: nginx:latest
        name: guestbook
        ports:
        - containerPort: 3000
          name: http
